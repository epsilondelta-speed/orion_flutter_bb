name: orion_flutter
description: Orion Flutter SDK (binary-only)
version: 1.2.4
flutter:
  plugin:
    platforms:
      android:
        package: co.epsilondelta.orion_flutter
        pluginClass: OrionFlutterPlugin
      ios:
        pluginClass: OrionFlutterPlugin

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter


